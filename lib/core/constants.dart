abstract class AppConstants {
  static const baseUrl = "https://dummyjson.com";
  static const users = "/users";
  static const posts = "/posts/user/{userId}";
  static const todos = "/todos/user/{userId}";
}
